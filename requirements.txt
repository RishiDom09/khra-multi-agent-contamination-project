# LangChain/LangGraph move fast with breaking changes — versions pinned so the
# public repo stays reproducible.
langchain==1.3.13
# 1.4.9 was the original pin; the provider clients below require >=1.5, and pip
# resolves the set to 1.5.3 (what is actually installed — verified 2026-08-03).
langchain-core==1.5.3
langchain-groq==1.1.3
langgraph==1.2.9
python-dotenv

# Multi-provider clients (AWS Bedrock + Azure AI Foundry), pinned from the
# first resolved install (pip freeze, 2026-08-01).
langchain-aws==1.6.4        # Bedrock native (Claude Opus 5) — ChatBedrockConverse
langchain-openai==1.4.1     # Bedrock's OpenAI-compatible endpoint (GPT-5.6 Sol)
langchain-azure-ai==1.2.8   # Azure AI Foundry serverless (DeepSeek V4-Pro, Grok 4.20)
